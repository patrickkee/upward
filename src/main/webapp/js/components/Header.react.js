
var React = require('react');

var Header = React.createClass({

  render: function() {
    return (
      <header id="header">
        <h1>upward.</h1>
      </header>
    );
  },

});

module.exports = Header;
